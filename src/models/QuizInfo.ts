type QuizInfo = {
    id: string,
    name: string,
    image?: string,
    creationDate?: string,
    updatedDate?: string,
    availableDate?: string,
    notesDocumentId?: string,
}

export default QuizInfo;
